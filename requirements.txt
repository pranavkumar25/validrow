# Kept for runners that read it. The shared backends (asyncpg, aioboto3, redis)
# are base dependencies in pyproject.toml, because Vercel resolved from that
# list and ignored this file, so anything named only here never got installed.
# `postgres` adds psycopg2, which only the Alembic CLI needs.
.[postgres]
